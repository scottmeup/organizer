# Traefik

Traefik is configured with static and dynamic configuration directories.

## Goals

- expose only webhook and selected UI surfaces
- keep internal services on the Docker network
- support path-based routing in the scaffold
