# Integrating with an Existing n8n Installation

Use `docker-compose.external-traefik-external-n8n.yml` if you already run n8n elsewhere.

Import the n8n workflow JSON files from this bundle into your current n8n instance and update endpoint URLs if that n8n does not resolve `http://org-core:8080` directly.
